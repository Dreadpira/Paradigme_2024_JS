const maskify = ("9583750923875498");

function mmasknum (num) {
  const last4Num = num.slice(-4); 
  const secret = "#".repeat(num.length 12);
  return `${secret}${last4Num}`;
}
console.log(maskNumber(maskify)); 

